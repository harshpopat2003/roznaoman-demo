import Building from "@/components/Building";

/**
 * Rozna is a building, so the site is one too.
 *
 * There are no marketing sections here — no hero, no benefits grid, no
 * comparison table, no testimonial rail, no footer. There are eight
 * rooms and seven doorways between them, walked in the order a guest
 * walks them, and each room holds only what is actually in that room.
 * The booking lives by the door out, because that is where you decide.
 */
export default function Home() {
  return <Building />;
}
